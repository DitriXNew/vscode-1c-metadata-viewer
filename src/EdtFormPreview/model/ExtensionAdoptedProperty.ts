// ExtensionAdoptedProperty - свойство адаптации расширения

/**
 * ExtensionAdoptedProperty - свойство адаптации расширения
 */
export interface ExtensionAdoptedProperty {
    /** Признак адаптированности */
    adopted?: boolean;
    
    /** Признак неизменённости */
    unchanged?: boolean;
}
