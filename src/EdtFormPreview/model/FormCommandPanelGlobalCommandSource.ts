// Соответствует com._1c.g5.v8.dt.form.model.FormCommandPanelGlobalCommandSource
import { FormStandardCommandSource } from './FormStandardCommandSource';

export interface FormCommandPanelGlobalCommandSource extends FormStandardCommandSource {
    // Источник глобальных команд панели команд формы
}
