// Соответствует com._1c.g5.v8.dt.form.model.FormStandardCommandSource
import { FormStandardCommand } from './FormStandardCommand';

export interface FormStandardCommandSource {
    commands: FormStandardCommand[];
    excludedCommands: FormStandardCommand[];
}
