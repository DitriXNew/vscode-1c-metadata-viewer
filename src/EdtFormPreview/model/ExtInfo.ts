/**
 * ExtInfo - базовый интерфейс для расширенной информации элементов
 * @see com._1c.g5.v8.dt.form.model.ExtInfo
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ExtInfo {
  // Базовый пустой интерфейс
}
