/**
 * FormItemContainer - контейнер элементов формы
 * Based on EDT com._1c.g5.v8.dt.form.model.FormItemContainer
 */

export interface FormItemContainer {
    items?: FormItem[];
}

import { FormItem } from './FormItem';
